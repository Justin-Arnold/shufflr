# Shufflr Configuration
# Copy this file to .env and update the values as needed

# Server Configuration
PORT=8080
BASE_URL=http://localhost:8080

# Database Configuration
DATABASE_PATH=./shufflr.db

# File Storage Configuration
UPLOAD_DIR=./uploads

# Security Configuration
# Generate a secure random string for production
# You can generate one with: openssl rand -hex 32
SESSION_SECRET=your-secure-session-secret-here

# Docker-specific overrides (when using docker-compose)
# Uncomment and modify these for Docker deployments
# DATABASE_PATH=/app/data/shufflr.db
# UPLOAD_DIR=/app/data/uploads

# Production Configuration Examples
# BASE_URL=https://images.yourdomain.com
# PORT=3000