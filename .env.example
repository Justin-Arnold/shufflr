# Shufflr Configuration
# Copy this file to .env and customize as needed

# Server Configuration
SHUFFLR_PORT=8080
SHUFFLR_BASE_URL=http://localhost:8080

# Data Directory (where database and uploads will be stored on host)
# This will be created on the host filesystem and mounted into the container
SHUFFLR_DATA_DIR=./shufflr-data

# Alternative: Separate directories for more control
# SHUFFLR_DATABASE_DIR=./shufflr-data
# SHUFFLR_UPLOAD_DIR_HOST=./shufflr-data/uploads

# Session Security (generate a random hex string)
# You can generate one with: openssl rand -hex 32
SHUFFLR_SESSION_SECRET=your-session-secret-here

# Advanced: Override internal container paths (usually not needed)
# SHUFFLR_DATABASE_PATH=/app/data/shufflr.db
# SHUFFLR_UPLOAD_DIR=/app/data/uploads